# 更新日志

## 1.0.1

- 统一配置页面与多模态 OCR 插件的主题边框、纵向卡片布局、间距和 GitHub 入口样式。
- 自定义请求体保留所有非消息字段，消息节点和提示词内容由插件统一生成。
- 记录被忽略的消息字段路径，允许用户覆盖 `model`、`stream` 和供应商扩展参数。
- 明确 `thinking` 必须按供应商协议放入请求体，不由插件强制修改。

## 1.0.0

- 提供基于大语言模型的纯文本翻译能力。
- 支持自定义 API 地址、API 密钥、模型 ID、请求体和请求头。
- 使用 STranslate 官方提示词机制，支持 `$source`、`$target` 和 `$content` 变量。
- 默认启用流式输出，并兼容非流式响应。
- 支持自动重试、自定义最大请求次数和自定义超时时间。
- 支持 OpenAI、Kimi、Qwen、MiniMax、Claude、Gemini 和其他常见语言模型协议。
- 提供真实连接校验、详细脱敏日志和完整错误信息。
